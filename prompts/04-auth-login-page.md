Use @specs/04-auth-login-page.md as the source of truth.

Implement only Section 04.

Scope: Next.js (auth) route group, public /login page with shadcn Card, POST /auth/login, and http-only apharma_token cookie via /api/auth/session.

Do not add register, logout, forgot-password, route guards, Google OAuth, or backend changes.

Update @context/tracker.md when done.
